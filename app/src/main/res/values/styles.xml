<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="android:windowBackground">@color/windowBackgroundColor</item>
        <item name="android:colorBackground">@color/windowBackgroundColor</item>
    </style>

    <style name="TextAppearance.ListItemTitle" parent="TextAppearance.AppCompat.Subhead">
        <item name="android:textColor">?android:textColorPrimary</item>
    </style>

    <style name="TextAppearance.ListItemTitle.Category">
        <item name="android:textStyle">bold</item>
    </style>

    <style name="row_item_style" parent="Widget.AppCompat.Button.Borderless">
        <item name="android:gravity">center_vertical</item>
        <item name="android:minHeight">60dp</item>
        <item name="android:paddingLeft">16dp</item>
        <item name="android:paddingRight">16dp</item>
        <item name="android:drawablePadding">32dp</item>
        <item name="android:textAppearance">@style/TextAppearance.ListItemTitle</item>
    </style>

    <style name="divider_style">
        <item name="android:layout_height">1dp</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:background">@color/dividerColor</item>
    </style>

    <style name="short_divider_style">
        <item name="android:layout_height">1dp</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_marginLeft">48dp</item>
        <item name="android:layout_marginStart" tools:targetApi="jelly_bean_mr1">48dp</item>
        <item name="android:background">@color/dividerColor</item>
    </style>

    <style name="categoryStyle">
        <item name="android:textAppearance">@style/TextAppearance.ListItemTitle.Category</item>
        <item name="android:minHeight">48dp</item>
        <item name="android:paddingLeft">16dp</item>
        <item name="android:paddingRight">16dp</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:background">@color/itemBackgroundColor</item>
    </style>

</resources>
